version: "3.9"

services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: production
      SECRET_KEY: change-me
      JWT_SECRET_KEY: change-me
