version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: healthguard-web
    env_file:
      - .env
    ports:
      - "5000:8080"
    restart: unless-stopped
